# Use the official ChromaDB image
FROM chromadb/chroma:latest

# Set environment variables for ChromaDB
ENV IS_PERSISTENT=TRUE
ENV ANONYMIZED_TELEMETRY=FALSE

EXPOSE 8000

# The official ChromaDB container handles the startup automatically
# No CMD needed - the base image provides the correct entrypoint