# If you're on the official image:
FROM chromadb/chroma:0.6.3
ENV CHROMA_SERVER_CORS_ALLOW_ORIGINS=[] \
    CHROMA_SERVER_HOST="::" \
    CHROMA_SERVER_HTTP_PORT=8000 \
    IS_PERSISTENT=TRUE \
    PERSIST_DIRECTORY=/data \
    ANONYMIZED_TELEMETRY=FALSE