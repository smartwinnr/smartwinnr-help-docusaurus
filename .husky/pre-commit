#!/usr/bin/env sh

# Run markdown linting on staged files (continue on errors)
npm run lint:docs:fix || echo "Markdown linting found issues but continuing..."

# Add any automatically fixed files back to staging
git add docs/**/*.md

echo "Pre-commit linting completed (non-blocking)"
